# W2 Retro

기간: 2026-05-13 ~ 2026-05-19 (7일 — plan 2주에서 단축)

## Shipped (9 PR, squash merged to main)

| PR | 제목 |
|---|---|
| #1 | ci(infra): introduce PR flow for W2 |
| #2 | feat(api): Redis client foundation |
| #3 | feat(api): multi-user signup with rate limit |
| #4 | feat(web): signup page |
| #5 | feat(api): categorization rulebook + redis cache + budget |
| #6 | feat(api): Claude Haiku categorization fallback |
| #7 | feat(api): parser registry + Woori card parser |
| #8 | feat(api): hana bank passbook parser (scope change) |
| #9 | chore(infra): W2 production rollout (Redis + ENV + docs) |

## What worked
- Spec → plan → 60 task → subagent-driven 실행 흐름. 각 Phase가 1 PR로 깔끔히 분리됨.
- Spec compliance + code quality 2단계 review가 매번 실제 버그 잡음 (KT 정규식 \b → lookahead, parsed.get 비-dict 위험, schema field 위치 등).
- categorization 모듈을 rulebook/cache/budget/service 4 파일로 분해 — LLM 추가가 service 한 곳에 한정됨.
- gh CLI 자동 squash merge로 Phase 사이 latency 최소화.

## What hurt
- 한국 카드사 `.xls` codepage 손상 문제 — xlrd 1.x/2.x 둘 다 무용. Excel COM 자동 변환 → mojibake. 결국 사용자 GUI 변환만 가능. **운영 정책**: production parser는 .xlsx만 받기.
- PowerShell stdout 인코딩(cp949)이 한글 표시 깨뜨려 분석 단계에서 시간 소모. `sys.stdout.reconfigure(encoding='utf-8')`로 해결.
- spec §9에 "통장 W4+"라고 했는데 Phase 7 시점에 사용자 보유 명세서 확인 결과 하나카드 없음 → 하나은행으로 scope 변경. **lesson**: spec brainstorming 단계에서 사용자 데이터 보유 현황 미리 묻기.
- Plan에서 `apps/web/src/pages/SignupPage.tsx` 가정했으나 실제는 `routes/login.tsx` 패턴 — Plan 작성 시 실제 디렉토리 구조 확인 부족.

## Numbers
- PR count: 9 (squash)
- Test count: 57 → 116 (+59)
- 마이그레이션: 1개 추가 (0002_add_llm_usage_log)
- Lines changed: 약 +2,200 / -100 (categorization 모듈, woori/hana 파서, signup, redis 인프라)
- LLM 호출 수 (테스트 시): 0 (모두 mock)
- 운영 LLM 비용: $0 → first user upload 후 측정 예정

## Carry into W3
- 대시보드 시각화 (월별/카테고리별 차트)
- 사용자별 카테고리 오버라이드 (전역 캐시 → user-specific) — 잘못 분류된 거래 수정
- 비밀번호 재설정 / 이메일 인증 (SMTP)
- 우리/하나 외 카드사 (현대, 신한, 국민, BC 등)
- 통장 거래내역에 대한 룰북 보강 (적요 패턴 — 월급, 월세, 정기적금 등)
- 사용자 본인 .xls → .xlsx 자동 변환 기능 (운영 측 부담 줄임)
