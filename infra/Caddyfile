api.spendlens.suim-app.store {
    reverse_proxy api:8000
    encode gzip zstd
    log {
        output file /var/log/caddy/access.log
        format json
    }
    request_body {
        max_size 10MB
    }
}
